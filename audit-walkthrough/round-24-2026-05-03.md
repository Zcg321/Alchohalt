# Round 24 polish — 2026-05-03

Sovereign mode. Owner has full decision authority. Branch
`claude/round-24-polish-2026-05-03` off `main` at `50c0cde`.

Round 24 thesis (owner directive 2026-05-03): "perfect and ready to
dominate its niche in user satisfaction and utility." This round adds
explicit competitive-benchmarking dimensions on top of the polish work
that has been compounding since round 1.

## Per-section status

| § | Theme | Status | Commit |
|---|-------|--------|--------|
| A | R24-A — Russian (ru) native-speaker judge (5/5 non-EN coverage) | ✅ Shipped | `8780daa` |
| B | R24-FF1 — "Try quick log?" hint after 7+ drinks logged | ✅ Shipped | `743a560` |
| C | R24-FF2 — Quick-mode "earlier today?" backdating link | ✅ Shipped | `743a560` |
| D | R24-FF3 — Less-prominent disclosure toggles for low-vision users | ✅ Shipped | `743a560` |
| E | R24-1 — Competitive feature parity matrix (5 × 30) | ✅ Shipped | `255f765` |
| F | R24-2 — Unique-to-us moat features doc | ✅ Shipped | `255f765` |
| G | R24-3 — On-device NPS pulse (every 30 days) | ✅ Shipped | `e334cfc` |
| H | R24-4 — Reading-grade comparison vs competitors | ✅ Shipped (in matrix § E) | `255f765` |
| I | R24-5 — Time-to-first-value expansion (Insight, Milestone, Crisis) | ✅ Shipped | `255f765` |
| J | R24-6 — 24th judge: UX researcher with $0 budget + study guide | ✅ Shipped | `255f765` |
| K | R24-7 — 24-judge spectacular gate + finalize | ✅ Shipped | `916dde1` |

Status legend: ⏳ planned, 🚧 in progress, ✅ shipped,
🛑 blocker / deferred.

## Voice-and-framing checklist

Per round-8 disagreement matrix D5: voice gates run per-round, not
per-feature. Walk every string added or modified this round through:

- [ ] No marketing voice (no exclamation, no empty modifiers)
- [ ] No "you're broken, this app fixes you" framing
- [ ] No clinical jargon the user is asked to apply to themselves
- [ ] No false certainty ("always", "never", "guaranteed")
- [ ] No second-person commands without context

If any string fails, fix it before the round merges.

## Disagreement-matrix consultation

Before adding a feature with conflicting judge inputs, check the
existing matrix in `round-8-eight-judges-2026-05-02.md` for precedent.
If the conflict is novel, add a new D-entry to that file with the
explicit decision and rationale.

## Round summary (filled at finalize)

### What landed

**Code (5 commits, 8 source files modified, 5 new):**

- `[R24-FF1]` `QuickLogHintBanner.tsx` — one-shot calm banner that
  surfaces in TrackTab when the user has logged ≥ 7 drinks in
  detailed mode. Two actions (switch / dismiss) both stamp
  `settings.quickLogHintAt` and the hint never returns.
- `[R24-FF2]` Quick-mode "Earlier today? Adjust last entry" link
  in `TrackTab.tsx`, visible when the most-recent drink was logged
  within the last 10 minutes. Routes to existing edit flow via
  `onStartEdit` — no parallel time-picker UI.
- `[R24-FF3]` Disclosure toggle in `TrackTab.tsx` stepped down
  from primary color to `text-ink-soft` + caret + aria-expanded.
- `[R24-A]` Russian translator audit fixes: 7 mechanical string
  corrections in `ru.json` + Slavic plural infrastructure
  (`pluralCount` routing) for `stats.softRestart.{restart,
  building}` and `progressCards.streak.{nextMilestone,
  daysFromThere}`. `.one|.few|.many|.other` keys added for pl + ru;
  `.one|.other` for en/de/es/fr (parity).
- `[R24-3]` `features/nps/` — pure helpers (`shouldShowNpsPrompt`,
  `clampScore`, `normalizeReason`, `bucketForScore`) +
  `NpsPulseBanner` (renders in TodayHome at 14-day floor + 30-day
  cadence) + `NpsFieldset` in `DiagnosticsAudit.tsx`.

**Docs (7 new files in `audit-walkthrough/`):**

- `round-24-baseline.md` (kickoff snapshot)
- `round-24-ru-translator-feedback.md` (8 blockers + consider
  list, ship-with-listed-fixes verdict)
- `round-24-competitive-matrix.md` (30 × 5 cell matrix + § B
  winners + § C losers + § D cheap wins + § E reading-grade + § F
  owner-blocking decisions)
- `round-24-moat-features.md` (8 moats + recommended top-5
  marketing rotation)
- `round-24-time-to-first-value.md` (TTFV-1..4 across 6 apps;
  Alchohalt average ~12s vs competitor average ~70s)
- `round-24-ux-researcher-judge.md` (pre-emptive audit + ready-
  to-run 1-week study guide)
- `round-24-twenty-four-judges-2026-05-03.md` (the gate;
  17 ship-hard verdicts across 5 surfaces)

### What didn't

- The Russian "consider list" (29 nit-level improvements beyond
  the 8 blockers) — cheap to apply but bundled into round 25 to
  keep this round's diff tightly scoped to blocker-level fixes.
- Country-aware std-drink units (matrix § D #1) — 3-day estimate,
  doesn't fit a single round.
- Derived calorie tile (matrix § D #2) — 2-day estimate, same.
- Global Hard-Time panel header entry (UX-researcher audit + TTFV
  follow-up) — small, but voice-sensitive, so deserves dedicated
  review.
- App Store description rewrite around top-5 moats — handed off to
  the owner with the moat-features + competitive-matrix docs.

### Findings filed for round 25

1. Country-aware std-drink units
2. Derived calorie tile
3. Global Hard-Time panel header entry (TTFV-4 + UX-researcher
   audit)
4. App Store description rewrite around top-5 moats
5. Quick-mode backdating window length tuning (currently 10 min;
   consider 30 min for end-of-night logging — judge 6 lens)
6. Quick-mode backdating link hit area (text-xs may be too small
   for fine-motor users — judge 5 conditional, await user reports)
7. Russian "consider list" polish (29 nit-level improvements)
8. Pin onboarding intent A/B variant before any external study
9. Insights empty-state preview state

### Baseline diff

- Tests: 1757 passed / 4 skipped → **1805 passed / 1 skipped**
  (+48 tests added; 3 previously-skipped now passing)
- Lint: 32 warnings (0 errors) → **32 warnings (0 errors)** (net
  zero — the 2 warnings introduced by R24 surfaces eliminated by
  the R24-7 sub-component extraction)
- Build: ✅ passes; bundle structurally unchanged (no new
  lazy-loaded chunks). `dist/assets/index-*.js` ~776 KB raw,
  `react-*.js` ~141 KB raw.
- Typecheck: ✅ clean
- Locale parity: ✅ 11 tests across `locale-parity.test.ts` +
  `locale-parity-all.test.ts`
