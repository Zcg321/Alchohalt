import * as Mod from "../capacitor-local-notifications.d.ts"; test("capacitor-local-notifications.d.ts exports callable", () => { for (const v of Object.values(Mod)) if (typeof v==="function"){ try{ (v as any)(); }catch{} } });
